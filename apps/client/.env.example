# Public server-side API origin used by the Next.js client.
API_URL=http://localhost:3001

# Secret used to encrypt the client session cookie. Set a random value (32+ chars) outside development.
SESSION_SECRET=
